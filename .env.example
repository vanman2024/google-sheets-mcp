# Google Sheets MCP Server Environment Variables
# Copy this file to .env and configure your settings

# =============================================================================
# GOOGLE OAUTH CREDENTIALS (Choose ONE method)
# =============================================================================

# METHOD 1: Environment Variables (RECOMMENDED FOR DEPLOYMENT)
# Get these from Google Cloud Console and your token file
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REFRESH_TOKEN=your_refresh_token_here

# METHOD 2: File-based (for local development only)
# Directory containing gcp-oauth.keys.json and sheets-token.json
# GDRIVE_CREDS_DIR=/path/to/credentials

# =============================================================================
# HTTP TRANSPORT SETTINGS
# =============================================================================

# Transport mode: stdio or http
MCP_TRANSPORT=stdio

# HTTP server host (for HTTP transport)
MCP_HOST=0.0.0.0

# HTTP server port (for HTTP transport)
MCP_PORT=8000

# Number of workers for production deployment
MCP_WORKERS=4

# CORS allowed origins (comma-separated)
# Example: http://localhost:3000,https://myapp.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
